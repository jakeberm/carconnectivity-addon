name: "CarConnectivity Add-on Edge"
version: 0.8.19
slug: "carconnectivity-addon-edge"
description: "⚠️EDGE VERSION⚠️ CarConnectivity Add-on Edge, may be unstable !"
image: "docker.io/jakeberm/carconnectivity-addon-edge-{arch}"
panel_icon: "mdi:car-wireless"
panel_title: "CarConnectivity"
map:
  - addon_config:rw
arch:
  - aarch64
  - amd64
ingress: true
init: false
hassio_api: true
hassio_role: homeassistant
homeassistant_api: true
# Configuration is done entirely in the addon Web UI (Ingress → /configui/).
# The single "info" option only exists to show a translated pointer on this
# otherwise empty Configuration tab; its value is never read by the addon.
options:
  info: "Infos → Web UI → Configuration"
schema:
  info: str
